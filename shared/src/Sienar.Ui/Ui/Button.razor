@inherits SienarComponentBase

@{
	Tag = IsLink ? "a" : "button";
}

<DynamicElement
	Tag="@Tag"
	Classes="@Css"
	@onclick="@(e => DelegateHandler.Handle(OnClick, e))"
	@attributes="UserAttributes">
	@ChildContent
</DynamicElement>
